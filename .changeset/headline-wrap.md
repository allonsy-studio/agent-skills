---
"@allons-y/agent-skills": patch
---

Wrap the landing-page headline at the slash (`@allons-y/` / `agent-skills`) instead of mid-word at a hyphen, so the package name breaks cleanly on narrow screens.
