---
"@allons-y/agent-skills": patch
---

Keep skill pages from overflowing on narrow viewports: long link URLs and inline code (like file paths) now wrap instead of pushing past the screen, the prose column is capped to its container width, and code blocks get a little inset so text no longer sits flush against their accent border.
